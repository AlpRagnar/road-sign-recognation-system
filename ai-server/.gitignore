# Python
__pycache__/
*.pyc
.venv/
venv/

# classes.json is a generated, committed artifact (see
# scripts/generate-traffic-sign-class-map.mjs) and must stay in sync with
# src/data/traffic-sign-classes.json — it is intentionally NOT ignored.
# classes.json.example remains as a small illustrative sample.
