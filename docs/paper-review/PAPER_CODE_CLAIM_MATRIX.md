# Paper ↔ Code Claim Matrix

Audit of **`IEEE_Traffic_Sign_Inventory_Updated_Draft.docx`** (title: *"A Server-Side Web Framework for Smartphone-Based Traffic Sign Detection, Geospatial De-duplication, and Inventory Management"*) against the repository at commit state of 2026-07-05.

> Note on filename: TASK 028 references `IEEE_Traffic_Sign_Inventory_Reviewer_Clean_Draft.docx`; the actual DOCX in the repository root is `IEEE_Traffic_Sign_Inventory_Updated_Draft.docx`. This audit uses the file that exists.

Classifications: `IMPLEMENTED`, `PARTIALLY_IMPLEMENTED`, `CONCEPTUAL_ONLY`, `FUTURE_WORK`, `OUTDATED`, `UNSUPPORTED`, `INCORRECT`.

| # | Paper section | Claim (quoted / paraphrased) | Classification | Code / schema / test evidence | Consistency | Required correction |
|---|---------------|------------------------------|----------------|-------------------------------|-------------|---------------------|
| 1 | Abstract; VI.A | "Next.js 14 … TypeScript, Tailwind CSS, Supabase Authentication, PostgreSQL, private object storage, row-level security, signed media access" | IMPLEMENTED | `package.json` `"next":"14.2.5"`; `src/app/layout.tsx` + `src/app/(protected)/…` (App Router); `tsconfig.json`; `tailwind.config.ts`; `src/lib/supabase/*`; `src/lib/storage/signed-urls.ts`; RLS in `supabase/migrations/0001_init.sql` L180–247 | Consistent | Keep |
| 2 | Abstract; IV.C; Index Terms | "exposes **change-layer** records"; Index Terms includes "change layer" | UNSUPPORTED | No `change_layer` / change-layer table in any migration; the only status mechanism is `traffic_signs.verification_status` (`0001_init.sql` L101) and `detection_events.validation_status` (L81). The only `*_candidates` table is `storage_quarantine_candidates` (`0005_storage_quarantine.sql` L38), which is storage garbage-collection, unrelated to inventory | Overstated | Reword to a **review-state workflow inspired by change-layer principles** on a single inventory table; remove "change layer" as an implemented component |
| 3 | Index Terms; II.C; X | "Supabase, **PostGIS**" (Index Terms); "PostGIS is identified as a scalable extension path" | FUTURE_WORK (Index Term is UNSUPPORTED) | No `postgis`/`geometry`/`geography`/`gist`/`ST_` in any migration (only `pgcrypto`, `0001` L6). Coordinates are `double precision`; indexes are btree only. `src/lib/localization/grouping.ts` L~189 comment: "at scale, replace with a PostGIS spatial index query" | II.C/X already frame it as a path; **Index Terms wrongly imply it is used** | Remove "PostGIS" from Index Terms; keep it only as future scalability in §X |
| 4 | V.B, Eq. (1) | "S_match = w1·S_spatial + w2·S_semantic + w3·S_direction + w4·S_road + w5·S_temporal" as the matching method | CONCEPTUAL_ONLY | No `S_match`, semantic/directional/road-context/temporal scoring anywhere. Actual matching in `src/lib/localization/grouping.ts` `groupDetectionIntoSign`: candidates fetched by **exact `sign_type` equality** (`.eq("sign_type", detected_class_name)`) then nearest within `env.signMatchRadiusMeters()` (default **25 m**, `src/lib/env.ts` L65). Only class + spatial radius | Not implemented as described | Move Eq. (1) to a **proposed extension / future work**, or replace with the actual same-class + 25 m nearest-neighbour rule |
| 5 | V.B, Eq. (2)(3) | "weight = confidence / max(gps_accuracy, 1)"; "refined_location = Σ(weight·position)/Σ(weight)" | IMPLEMENTED (with documented defaults) | `src/lib/localization/grouping.ts` `weightedAverage` L24–40: `conf = r.confidence ?? 0.5`; `accuracy = Math.max(r.accuracy ?? 1, 1)`; `w = conf/accuracy`; returns `Σ(w·value)/Σ(w)`, simple-mean fallback when `Σw==0`. Latitude and longitude are weighted **independently** (separate `latPoints`/`lonPoints`). Re-applied after deletion by `recomputeSignAggregate` L61–152 | Matches, with two nuances not stated in the paper | Keep Eq. (2)(3); add a sentence: null confidence defaults to 0.5, null accuracy to 1, and lat/lon are fused independently |
| 6 | III; V.C | "estimate a **stable location for the physical sign**"; "refines the representative position of a sign" | PARTIALLY_IMPLEMENTED | No camera calibration, bounding-box geometry, heading projection, depth, triangulation, or multi-view reconstruction. `heading`/`speed` are stored (`detection_events`, `device_location_logs`) but **never used in matching or localization** (grep of `grouping.ts` = none). Location = confidence/accuracy-weighted mean of **device GPS** coordinates | Risk of implying geometry-derived localization | State explicitly: device GPS is used as an **observation-location proxy**; the representative coordinate is a fused centroid of device positions, not a geometrically reconstructed sign position |
| 7 | III | "preserve **complete raw detection history** so that administrative decisions can be inspected later" | PARTIALLY_IMPLEMENTED / OUTDATED | Raw `detection_events` + `ai_response_raw` are retained by default, but admin-only permanent deletion exists: `src/app/api/admin/detections/[id]/frame/route.ts` DELETE removes the whole frame group (events, observations, orphan signs, storage object) with audit action `ADMIN_FRAME_DELETED` | Contradicted by permanent deletion | Reword: raw history is retained **by default for audit**; authorized admins may permanently remove erroneous frames + dependent records; each deletion writes an audit record and storage deletion is reference-safe |
| 8 | IV.C; VIII | "Administrators can **approve, reject, merge, split, correct, or mark** candidates" | INCORRECT (merge/split/correct) | Implemented actions: Verify→`manually_verified`, Reject→`rejected`, Duplicate→`duplicate`, Reset→`pending` (`src/components/AdminDetectionsClient.tsx` L22–28; `src/app/(protected)/admin/review/ReviewClient.tsx` L6–10; `src/app/api/admin/review-detection/route.ts`), plus permanent frame deletion. **No merge, split, correct-class, relocation, or damage-report action** exists (repo grep = none) | Overstated | Replace with the actual set: verify, reject, mark duplicate, reset to pending, permanently delete frame; move merge/split/correct to future work |
| 9 | III; IV.B; V; Table I; Table III | "raw observations, **candidates**, fused sign instances, **validated sign instances**" as distinct entities | PARTIALLY_IMPLEMENTED (terminology) | Schema has `detection_events` (raw) → `traffic_sign_observations` (link) → `traffic_signs` (one inventory row, status via `verification_status`). There is **no separate `candidates` table and no separate `validated_sign_instances` table** | Multiple conceptual stages map to one table + statuses | Map terms explicitly: candidate/fused/validated stages are **review states of a single `traffic_signs` record**, not separate tables |
| 10 | VI.A | "Next.js 14 with the App Router … Server Components and Route Handlers … Supabase … RLS" | IMPLEMENTED | As row 1; Route Handlers under `src/app/api/**/route.ts` (43 files); RLS `0001_init.sql` | Consistent | Keep |
| 11 | VI.A | "migrations 0001-0006, covering core tables, secure image object paths, legacy path backfill, analytics RPC functions, storage quarantine governance, and daily metrics snapshots" | IMPLEMENTED | `supabase/migrations/` = `0001_init.sql`, `0002_secure_image_paths.sql`, `0003_backfill_image_paths.sql`, `0004_analytics_rpc.sql`, `0005_storage_quarantine.sql`, `0006_daily_metrics_snapshots.sql` | Exact match | Keep |
| 12 | IV.A; VI.A; Table I | "supports a real external detector and a deterministic mock detector … same normalized structure" | IMPLEMENTED | `src/lib/ai/client.ts` `runDetection` modes mock/external/auto; `src/lib/ai/contract.ts` `normalizeAiResponse` (detections array; confidence ∈ [0,1]; non-empty `class_name`; bbox validated) | Consistent | Keep; but ADD the concrete evaluated pipeline (row 13) |
| 13 | (missing) | The evaluated prototype's actual detector pipeline | IMPLEMENTED but UNDER-DESCRIBED | `ai-server/main.py`: FastAPI wrapper, `GET /health` + `POST /detect`; NVIDIA Triton client; two models `e2e` (detection) + `sign-mid` (classification); `DET_IMGSZ=1280`, `CLS_IMGSZ=128`; NMS (`_nms_and_scale`, ultralytics or numpy fallback); `confidence = det_conf × top1_conf` clamped to [0,1]; bearer auth `_check_auth` | Missing from paper | ADD a short subsection: contract-oriented adapter **plus** a Triton-backed two-stage prototype; do **not** claim detector accuracy |
| 14 | (missing) | 400-class friendly class mapping | IMPLEMENTED | `classifier_index_classes_mapping.yaml`; `scripts/generate-traffic-sign-class-map.mjs`; `src/data/traffic-sign-classes.json` and `ai-server/classes.json` (**both exactly 400 keys**); resolver `getTrafficSignDisplayName` in `src/lib/traffic-sign-classes.ts`; wrapper fallback `f"Sign {class_id}"` (`ai-server/main.py`) | Missing from paper | Mention 400-class mapping + friendly resolver where class labels are discussed |
| 15 | VI.A | "confidence values must lie in [0,1], class names must be non-empty, and bounding boxes must be well-formed … timeouts and bounded retry" | IMPLEMENTED | `src/lib/ai/contract.ts` L125–154; retry/timeout in `src/lib/ai/client.ts` + `src/lib/env.ts` `AI_MODEL_TIMEOUT_MS`/`AI_MODEL_MAX_RETRIES`/`AI_MODEL_RETRY_BACKOFF_MS` | Consistent | Keep |
| 16 | VI.B; VIII | "inventory map, sign detail panels, detection review tables, device views, analytics, presentation mode, AI integration health checks, storage governance" | IMPLEMENTED | `src/components/TrafficSignMap.tsx` (markers/clustered/density via `src/lib/cluster.ts`), `SignDetailPanel.tsx`, `AdminDetectionsClient.tsx`, `LiveDevicesMap.tsx`, `AdminAnalyticsClient.tsx`, `presentation/page.tsx`, `admin/ai/page.tsx` (health/self-test), `AdminStorageClient.tsx` | Consistent | Keep |
| 17 | VI.B | "database stores object paths rather than permanent public URLs … short-lived signed URL … CSV exports include storage object references and availability status rather than reusable signed URLs" | IMPLEMENTED | `0002_secure_image_paths.sql`; `src/lib/storage/signed-urls.ts`; `src/app/api/admin/export/*.csv/route.ts` emit `image_path` + `image_available`, not signed URLs | Consistent | Keep |
| 18 | VIII; Table IV | "The end-to-end Playwright suite passed **24 of 24** cases … **54 application routes** built" | OUTDATED | Current suite ≈ 61 authored `test()` across 10 specs in `tests/e2e/` (chromium + a WebKit project in `playwright.config.ts`); production build now reports **62 routes** (2 static, 60 dynamic). See Phase-11 run for the current pass count | Numbers stale | Replace with the freshly measured pass count and 62 routes, or state "full E2E suite passes; N routes built" from the current run |
| 19 | VIII; Table IV | "seeded run contains 4 devices, 6 sessions, 120 detection events, 35 signs, and 7 daily metric snapshots" | IMPLEMENTED (accurate) | `src/lib/demo/seed.ts`: `DEVICE_SPECS` (4), sessions `Array.from({length:6})` L271, `EVENT_COUNT=120` L342, signs `Array.from({length:35})` L293, 7 daily snapshot dates | Accurate | Keep; label clearly as seeded demo data, not measurements |
| 20 | VIII | "35 signs" presented near detector context | IMPLEMENTED | As row 19 — these are **seeded** counts demonstrating data flow | Correct as long as framed as demo | Keep the paper's existing caveat "not detection-performance measurements" |
| 21 | VII; VIII | "capture-to-upload time, upload latency, detection API response time, end-to-end processing time … localization mean/median error … duplicate reduction ratio" reported as results | CONCEPTUAL_ONLY (as metrics defined); UNSUPPORTED if reported as measured | `ai_response_time_ms` is stored per event (`detection_events`) and shown in analytics, but no measured latency/localization/dedup values are computed or reported. No labelled ground truth in repo | Metrics are defined, not measured | Keep §VII as an evaluation **protocol**; ensure §VIII reports only implementation validation, not measured algorithmic performance |
| 22 | IV/VI; Security | "secrets stay on the server … object storage remains private … administrative operations are role-gated" | IMPLEMENTED | `src/lib/supabase/admin.ts` service-role client (server only); admin routes guard `ctx.profile.role !== "admin"`; private bucket + signed URLs; AI bearer `_check_auth` | Consistent | Keep |
| 23 | (implicit) | Rate limiting | UNSUPPORTED | No rate-limiting / throttle middleware anywhere (grep `rate|throttle` → only `failure_rate` metric). "Bounded external API calls" = retry caps, not client rate limiting | Paper does not explicitly claim rate limiting — do not add it | Do not introduce a rate-limiting claim |
| 24 | (missing) | Device stays active after stopping a session; single-flight mobile capture | IMPLEMENTED | `src/app/api/detection/session/stop/route.ts` no longer flips device status; device status is admin-only (`src/app/api/devices/[id]/route.ts`); single-flight + AbortController in `src/app/(protected)/detection/DetectionClient.tsx` + `src/lib/detection/live-results.ts` | Missing (optional) | Optionally mention single-flight capture in the performance/methodology discussion; keep concise |
| 25 | References | Reference [18] (WHO Global Status Report on Road Safety 2023) | INCORRECT (uncited) | `[18]` appears in the reference list but is **never cited in the body text** (only [1]–[17] are cited). See `PAPER_REFERENCE_AUDIT.md` | Uncited reference | Either cite [18] in the Introduction (road-safety motivation) or remove it |
